import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
  console.log('🚀 Utils package tests starting...');
});

afterAll(() => {
  console.log('✅ Utils package tests completed!');
});

console.log('📦 Utils package test setup loaded');
