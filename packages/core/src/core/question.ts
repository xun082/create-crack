import { PROJECT_TYPE_OPTIONS, PACKAGE_MANAGER_OPTIONS } from './constants';

// 为了向后兼容，保留原有的导出名称
export const ProjectTypes = PROJECT_TYPE_OPTIONS;
export const PackageManagers = PACKAGE_MANAGER_OPTIONS;
