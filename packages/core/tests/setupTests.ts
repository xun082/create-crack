import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
  console.log('🚀 Core package tests starting...');
});

afterAll(() => {
  console.log('✅ Core package tests completed!');
});

console.log('📦 Core package test setup loaded');
